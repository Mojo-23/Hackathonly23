# NO ACTIVE TASK

There is no active task right now. Codex must not be run against this file.

Claude writes a real task here (using the shape in `tasks/TASK_TEMPLATE.md`) when there is work ready to hand off. Until then, this placeholder stays in place. Completed tasks are archived under `tasks/archive/`.

`scripts/run-codex-task.ps1` refuses to launch Codex while this file starts with `# NO ACTIVE TASK` — this is expected behavior, not a bug.
